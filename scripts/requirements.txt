# Translation Script Dependencies

# DeepL API client
deepl==1.19.1

# YAML frontmatter parsing
python-frontmatter==1.1.0

# Environment variable management
python-dotenv==1.0.1

# Beautiful terminal output
rich==13.9.4

# Sitemap Validation Dependencies

# HTTP requests with retry logic (legacy, kept for compatibility)
requests==2.32.3

# Async HTTP client for parallel requests (primary)
httpx==0.27.0

# XML parsing (built-in to Python, listed for reference)
# xml.etree.ElementTree